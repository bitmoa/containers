# NOTE: The kserve binaries do not have a --version flag, so we cannot perform a version check
command:
  check-help:
    exec:
      - /opt/bitmoa/kserve/bin/manager
      - --help
    exit-status: 0
    stderr:
      - kserve controller manager
