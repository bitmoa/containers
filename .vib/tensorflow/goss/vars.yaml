binaries:
   - python
root_dir: /opt/bitmoa
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitmoa/python
version:
  bin_name: python
  flag: "-c 'import tensorflow; print(tensorflow.__version__)'"
  timeout: 60000
