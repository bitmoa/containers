# Copyright Broadcom, Inc. All Rights Reserved.
# SPDX-License-Identifier: APACHE-2.0

file:
  # Checks default config for erlang is set
  /opt/bitmoa/ksql/etc/ksqldb/ksql-server.properties.default:
    exists: true
    filetype: file
    contents:
      - "http://0.0.0.0:8088"
      - "ksql.streams.state.dir = /bitmoa/ksql/data"
  # ksql does not have a version flag so we check the version.txt
  /opt/bitmoa/ksql/share/doc/ksqldb/version.txt:
    exists: true
    contents:
      - "{{ .Env.APP_VERSION }}"
