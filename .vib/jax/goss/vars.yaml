binaries:
  - python
root_dir: /opt/bitmoa
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitmoa/python
version:
  bin_name: python
  flag: "-c 'import jax; print(jax.__version__)'"
