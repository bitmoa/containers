binaries:
  - python
  - mlflow
root_dir: /opt/bitmoa
directories:
  - mode: "0755"
    paths:
      - /opt/bitmoa/python
  - mode: "0775"
    paths:
      - /app
version:
  bin_name: python
  flag: "-c 'import mlflow; print(mlflow.__version__)'"
