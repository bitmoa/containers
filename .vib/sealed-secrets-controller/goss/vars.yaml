files:
  - mode: "0644"
    paths:
      - /opt/bitmoa/sealed-secrets/.spdx-sealed-secrets.spdx
  - mode: "0755"
    paths:
      - /opt/bitmoa/sealed-secrets/bin/controller
version:
  bin_name: controller
  flag: --version
