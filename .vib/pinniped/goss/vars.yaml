files:
  - mode: "0644"
    paths:
      - /opt/bitmoa/pinniped/.spdx-pinniped.spdx
  - mode: "0755"
    paths:
      - /opt/bitmoa/pinniped/bin/pinniped-concierge-kube-cert-agent
      - /opt/bitmoa/pinniped/bin/pinniped-server
version:
  bin_name: pinniped-concierge
  flag: --help
