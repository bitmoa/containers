binaries:
  - memcached_exporter
files:
  - mode: "0644"
    paths:
      - /opt/bitmoa/memcached-exporter/.spdx-memcached-exporter.spdx
  - mode: "0755"
    paths:
      - /opt/bitmoa/memcached-exporter/bin/memcached_exporter
root_dir: /opt/bitmoa
version:
  bin_name: memcached_exporter
  flag: --version
