binaries:
  - node
  - yarn
  - npm
root_dir: /opt/bitmoa
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitmoa/attu/dist
      - /opt/bitmoa/attu/build
version:
  bin_name: cd /opt/bitmoa/attu; npm
  flag: list /opt/bitmoa/attu || true
