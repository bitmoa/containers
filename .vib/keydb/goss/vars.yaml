binaries:
  - keydb-cli
  - keydb-server
  - wait-for-port
directories:
  - mode: "0775"
    paths:
      - /bitmoa
      - /bitmoa/keydb
      - /bitmoa/keydb/data
      - /opt/bitmoa/keydb
      - /opt/bitmoa/keydb/etc
root_dir: /opt/bitmoa
version:
  bin_name: keydb-server
  flag: --version
