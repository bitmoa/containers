binaries:
  - airflow
  - ini-file
  - python
  - wait-for-port
linked_libraries:
  exclude_paths:
    # Ignore python venv executable scripts
    - /opt/bitmoa/airflow/venv/.*
sed_in_place:
  exclude_paths:
    # Ignore python venv executable scripts
    - /opt/bitmoa/airflow/venv/.*
directories:
  - mode: "0775"
    paths:
      - /opt/bitmoa/airflow
      - /opt/bitmoa/airflow/dags
      - /opt/bitmoa/airflow/logs
      - /opt/bitmoa/airflow/logs/scheduler
      - /opt/bitmoa/airflow/tmp
root_dir: /opt/bitmoa
version:
  bin_name: airflow
  flag: version
  timeout: 30000
subpackages:
  - amazon
  - celery
  - docker
  - elasticsearch
  - google
  - hashicorp
  - ldap
  - mysql
  - postgres
  - redis
  - statsd
