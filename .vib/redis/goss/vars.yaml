binaries:
  - redis-cli
  - redis-server
  - wait-for-port
directories:
  - mode: "0775"
    paths:
      - /bitmoa
      - /bitmoa/redis
      - /bitmoa/redis/data
      - /opt/bitmoa/redis
root_dir: /opt/bitmoa
version:
  bin_name: redis-server
  flag: --version
