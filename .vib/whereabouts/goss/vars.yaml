binaries:
  - whereabouts
  - ip-control-loop
  - install-cni.sh
files:
  - mode: "0755"
    paths:
      - /opt/bitmoa/whereabouts/bin/install-cni.sh
root_dir: /opt/bitmoa
version:
  bin_name: whereabouts
  flag: --version
