binaries:
  - fluent-bit
files:
  - paths:
    - /opt/bitmoa/fluent-bit/conf/fluent-bit.conf
    - /opt/bitmoa/fluent-bit/conf/parsers.conf
    - /opt/bitmoa/fluent-bit/conf/parsers_java.conf
directories:
  - paths:
      - /opt/bitmoa/fluent-bit/conf
      - /opt/bitmoa/fluent-bit/log
root_dir: /opt/bitmoa
version:
  bin_name: fluent-bit
  flag: --version
