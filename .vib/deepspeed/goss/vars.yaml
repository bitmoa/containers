binaries:
   - python
   - deepspeed
root_dir: /opt/bitmoa
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitmoa/python
version:
  bin_name: python
  flag: "-c 'import deepspeed; print(deepspeed.__version__)'"
  timeout: 60000
