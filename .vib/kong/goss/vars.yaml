binaries:
  - kong
directories:
  - mode: "0775"
    paths:
      - /opt/bitmoa/kong/server
      - /opt/bitmoa/kong/conf
  - paths:
      - /docker-entrypoint-initdb.d
      - /opt/bitmoa/kong/bin
root_dir: /opt/bitmoa
